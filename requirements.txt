# ============================================================
# OptiStore — Global Requirements
# ============================================================
# This file is the single reference for all project dependencies.
# Backend uses Python packages (pip), frontend uses Node packages (npm).
#
# Setup:
#   Backend:  cd backend && pip install -r requirements.txt
#   Frontend: cd frontend && npm install
# ============================================================


# ------------------------------------------------------------
# BACKEND — Python 3.11+
# ------------------------------------------------------------

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1          # ASGI server with hot-reload

# Database
sqlalchemy==2.0.30                 # ORM
alembic==1.13.1                    # migrations
psycopg2-binary==2.9.9             # PostgreSQL driver

# Config & validation
python-dotenv==1.0.1
pydantic==2.7.1
pydantic-settings==2.2.1

# Auth & security
python-jose[cryptography]==3.3.0   # JWT
passlib[argon2]==1.7.4             # password hashing (argon2)
python-multipart==0.0.9            # form data parsing

# Rate limiting
slowapi==0.1.9

# PDF generation
reportlab==4.2.2                   # invoice PDF (A4 + thermal)
Pillow==10.4.0                     # image support for barcode labels

# Barcode generation
python-barcode[images]==0.15.1     # Code128 barcodes

# Excel export
openpyxl==3.1.5                    # .xlsx generation (server-side)

# Testing
pytest==8.2.0
pytest-asyncio==0.23.7
httpx==0.27.0                      # async HTTP client for tests


# ------------------------------------------------------------
# FRONTEND — Node.js 18+  (managed via frontend/package.json)
# ------------------------------------------------------------
# Run: cd frontend && npm install
#
# Production dependencies:
#   next@14.2.5              Next.js App Router
#   react@18.3.1
#   react-dom@18.3.1
#   react-hook-form@7.52.1   form state + validation
#   swr@2.2.5                data fetching / cache
#   recharts@2.12.7          charts (admin dashboard)
#   jsbarcode@3.11.6         client-side barcode rendering (display only)
#   clsx@2.1.1               conditional classNames
#   date-fns@3.6.0           date formatting
#
# Dev dependencies:
#   typescript@5.5.3
#   tailwindcss@3.4.4
#   autoprefixer@10.4.19
#   postcss@8.4.39
#   eslint@8.57.0
#   eslint-config-next@14.2.5
#   @types/node@20.14.9
#   @types/react@18.3.3
#   @types/react-dom@18.3.0
